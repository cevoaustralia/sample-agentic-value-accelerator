# =============================================================================
# Base Requirements - Required for all deployment patterns
# =============================================================================

# Core Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0

# LangChain & LangGraph - pinned versions that work together
langchain==0.3.14
langchain-aws==0.2.10
langchain-core==0.3.29
langgraph==0.2.60

# AWS SDK (compatible with langchain-aws 0.2.10)
boto3>=1.35.74
botocore>=1.35.74

# Utilities
structlog>=24.4.0
python-dotenv>=1.0.1
httpx==0.27.2

# =============================================================================
# Optional Dependencies
# =============================================================================
# For AgentCore deployment: pip install -r requirements.txt -r requirements_agentcore.txt
# For Lambda deployment: pip install -r requirements.txt -r requirements_lambda.txt
# For development: pip install -r requirements.txt -r requirements_dev.txt
